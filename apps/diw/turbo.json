{
	"extends": [
		"//"
	],
	"tasks": {
		"build": {
			"env": [
				"$TURBO_EXTENDS$",
				"DATABASE_CONNECTION_STRING",
				"AUTH_BASE_URL",
				"APP_BASE_URL",
				"AUTH_SERVICE_TOKEN"
			]
		}
	}
}
